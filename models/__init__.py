"""Архитектуры нейросетей проекта (3D U-Net через MONAI), реестр моделей по модальности
и интеграция готовых предобученных моделей (models.pretrained_models).

Пакет намеренно не делает eager-реэкспорт символов из своих подмодулей: models.unet3d
и models.registry требуют torch/monai, а models.pretrained_models спроектирован так,
чтобы часть его функциональности (реестр, детектор ресурсов, фильтр Франги) работала
и без этих тяжёлых зависимостей — eager-импорт здесь свёл бы это на нет. Импортируйте
нужный подмодуль напрямую, например: `from models.unet3d import build_unet3d`.
"""
