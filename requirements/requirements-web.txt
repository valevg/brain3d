# web/ — это Three.js/WebGL фронтенд на чистом JavaScript, его зависимости управляются
# через npm и описаны в web/package.json (three, vite), а не через pip.
#
# Этот файл существует для единообразия структуры requirements/ и предназначен для
# ВСПОМОГАТЕЛЬНЫХ Python-инструментов, которые могут понадобиться при работе с web/
# (например, для локальной раздачи собранного статического билда без npm/node).
# Прямых зависимостей ядра (core/models/pipelines) здесь нет и быть не должно.

# Локальный статический сервер для web/dist (опционально, для быстрой проверки прод-сборки)
starlette==0.37.2
uvicorn[standard]==0.30.1
