# Зависимости FastAPI backend (api/). Устанавливать поверх requirements-common.txt:
#   pip install -r requirements/requirements-common.txt -r requirements/requirements-api.txt
-r requirements-common.txt

fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Rate limiting для /api/models/download, /api/process, /api/cache/clear (api/routes.py)
slowapi==0.1.10
